# Config file for the lekhika-core package
# It defines the imported target liblekhika::liblekhika

@PACKAGE_INIT@

# Find dependencies
find_dependency(SQLite3)

if (NOT TARGET liblekhika::liblekhika)
    include("${CMAKE_CURRENT_LIST_DIR}/lekhika-core-targets.cmake")
endif()
