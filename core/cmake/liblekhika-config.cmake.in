# Config file for the liblekhika package
# It defines the imported target liblekhika::liblekhika

@PACKAGE_INIT@

# Find dependencies
find_dependency(SQLite3)

if (NOT TARGET liblekhika::liblekhika)
    include("${CMAKE_CURRENT_LIST_DIR}/liblekhika-targets.cmake")
endif()
